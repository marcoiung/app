export interface ISlide {
  idx:number;
  src: string;
  icon: string;
  h1: string;
  h2: string;
  alt: string;
  btnText: string;
}

export interface IIndicators {
  id: string;
  index: number;
}
